# === Application Info ===
spring.application.name=springboot

# === Database Configuration ===
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/makeup_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=daiviknaik
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA / Hibernate Settings ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# === Server Settings ===
server.port=8080

# === Optional: Enable Open-in-View (for lazy loading) ===
spring.jpa.open-in-view=true

# === Logging Hibernate SQL (Optional Debugging) ===
logging.level.org.hibernate.SQL=DEBUG
